new GapList()
new GapList(int capacity)
new GapList(Collection<E>)

GapList.create()
GapList.create(Collection<E>)
GapList.create(E ...)

clear()
resize(size, elem)

addAll(collection), addAll(int, collection)
addArray(...),      addArray(int, ...)
addMult(num, elem), addMult(int, num, elem)

setAll(index, collection)
setArray(index, ...)
setMult(index, num, elem)

putAll(index, collection)
putArray(index, ...)
putMult(index, num, elem)

initAll(collection)
initArray(...)
initMult(num, elem)

replaceAll(collection)
replaceArray(...)
replaceMult(num, elem)

replace(index, 0, collection) = addAll(index, collection)
replace(index, collection.size, collection) = setAll(index, collection)
replace(index, -1, collection) = putAll(index, collection)
replace(0, this.size, collection) = initAll(collection)
replace(index, length, null) = remove(index, length)


Single element:
get(index), remove(index), set(index, value), add(value), add(index, value),

Multiple elements:


E[] getArray(index, length) --> DELETE

List get(index, length)
List getWhere(cond)

-- remove elements and return them
List extract(index, length)
List extractWhere(cond)

-- set element position to null and return them
List erase(index, length)
List eraseWhere(cond)

void remove(index, length)
void removeAll(Collection)
void removeWhere(cond)

void retain(index, length)
void retainAll(Collection)
void retainWhere(cond)


//

void reverse()
void revese(index, len)

void rotate(dist)
void rotate(index, len, dist)

// Copy / Move / Drag / Swap
// Size of list does not change
void copy(int srcIndex, int dstIndex, int len)
void move(int srcIndex, int dstIndex, int len)
void drag(int srcIndex, int dstIndex, int len)
void swap(int index1, int index2, int len)

static void transferCopy(src, srcIndex, srcLen, dst, dstIndex, dstLen)
static void transferMove(src, srcIndex, srcLen, dst, dstIndex, dstLen)
static void transferRemove(src, srcIndex, srcLen, dst, dstIndex, dstLen)
static void transferSwap(src, srcIndex, dst, dstIndex, len)

source part:
- copy: leave unchanged
- move: set to null
- drag: remove

destination part:
- add
- set
- put


